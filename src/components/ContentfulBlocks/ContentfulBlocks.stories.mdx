import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { ContentfulBlocks } from ".";

<Meta
  title="contentful/Components/ContentfulBlocks"
  component={ContentfulBlocks}
/>

# ContentfulBlocks

The `ContentfulBlocks` component is used to consume the content blocks that are created by Contentful. This is the main way that pages on the website are built.

## Usage

```tsx
import { ContentfulBlocks } from "@/components/ContentfulBlocks";
```

## Props

<ArgsTable component={ContentfulBlocks} />
