import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { Caption } from ".";

<Meta
  title="components/Caption"
  component={Caption}
  parameters={{
    viewMode: "docs",
  }}
/>

# Caption

A `Caption` is reserved and used for a short amount of text to describe an upcoming piece of content. A `Caption` is similar in usage to a `Heading` component.

## Usage

```tsx
import { Caption } from "@/components/Caption";
```

## Props

<ArgsTable component={Caption} />

## Example

<Canvas>
  <Story name="Default" args={{ title: "Robots are the future" }}>
    {(args) => <Caption {...args} />}
  </Story>
</Canvas>
