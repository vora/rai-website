import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { Logo } from ".";

<Meta title="components/Logo" component={Logo} />

# Logo

The `Logo` component renders an `svg` that displays the Responsible AI logo.

## Usage

```tsx
import { Logo } from "@/components/Logo";
```

## Props

<ArgsTable component={Logo} />

## Playground

<Canvas>
  <Story name="Playground">
    {(args) => <Logo {...args} />}
  </Story>
</Canvas>
