import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { Icon } from ".";
import * as featherIcons from "react-feather";
import { customIcons } from "./customIcons";

<Meta
  title="components/Icon"
  component={Icon}
  parameters={{
    viewMode: "docs",
  }}
/>

export function Icons({ icons, size = "base" }) {
  return (
    <table>
      <thead>
        <tr>
          <th>Icon</th>
          <th>Value</th>
        </tr>
      </thead>
      <tbody>
        {Object.keys(icons).map((icon) => (
          <tr key={icon}>
            <td>
              <Icon icon={icon} size={size} />
            </td>
            <td>{icon}</td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}

# Icon

The `Icon` component is a wrapper around `react-feather` and uses [feather icons](https://feathericons.com).

An `Icon` should be used to display small visual aids to micro-content.

## Usage

```tsx
import { Icon } from "@/components/Icon";
```

## Props

<ArgsTable component={Icon} />

## Example

<Canvas>
  <Story name="Default" args={{ icon: "Coffee" }}>
    {(args) => <Icon {...args} />}
  </Story>
</Canvas>

## Sizes

The `Icon` should be sized using the `size` prop and not through CSS. This allows for a more consistent experience throughout the website.

<Canvas>
  <Story name="Sizes" args={{ icon: "Command", size: "large" }}>
    {(args) => <Icon {...args} />}
  </Story>
</Canvas>

## All Icons

<Icons icons={featherIcons} />

## Custom Icons

<Icons icons={customIcons} size="largest" />
