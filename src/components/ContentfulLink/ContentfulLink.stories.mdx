import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { ContentfulLink } from ".";

<Meta title="contentful/Components/ContentfulLink" component={ContentfulLink} />

# Button

`ContentfulLink` is used to render a `Link` based on the contentful button response.

## Usage

```tsx
import { ContentfulLink } from "@/components/ContentfulLink";
```

## Props

<ArgsTable component={ContentfulLink} />

## Example

<Canvas>
  <Story
    name="Default"
    args={{
      action: {
        title: "Roger, roger",
        external: false,
        entrySlug: "/foo",
        enabled: true,
        variation: "default",
      },
    }}
  >
    {(args) => <ContentfulLink {...args} />}
  </Story>
</Canvas>
