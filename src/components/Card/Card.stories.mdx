import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { Card } from "./Card";
import { dummyContent } from "@/components/RichText/__mocks__/mockRichText";

<Meta
  title="components/Card"
  component={Card}
  parameters={{
    viewMode: "docs",
  }}
/>

# Card

## Usage

```tsx
import { Card } from "@/components/Card";
```

## Props

<ArgsTable component={Card} />

## Fragment

The `Card` component exposes a fragment `CardFragment`

```gql
query GetSomeData {
  ...CardFragment
}
```

## Example

<Canvas>
  <Story
    name="Default"
    args={{
      icon: "RAI Badge",
      title: "This is a title",
      content: dummyContent,
      link: {
        __typename: "ContentfulResource",
        url: "https://eddysims.com",
        title: "Eddy Sims",
      },
    }}
  >
    {(args) => <Card {...args}>Are Droids Allowed Here?</Card>}
  </Story>
</Canvas>
