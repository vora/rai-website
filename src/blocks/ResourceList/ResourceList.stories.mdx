import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { ResourceList } from ".";
import { ListAllResourceListBlocks } from "./ListAllResourceListBlocks";

<Meta
  title="contentful/blocks/ResourceList"
  component={ResourceList}
  parameters={{
    viewMode: "docs",
  }}
/>

# ResourceList

The `ResourceList` is a block that is used to show a grid style display of resources. These resources can come in the form of a Responsible AI blog post or an external resource cited somewhere on the internet.

## Usage

```tsx
import { ResourceList } from "@/blocks/ResourceList";
```

## Props

<ArgsTable component={ResourceList} />

## Example

<Canvas>
  <Story name="All ResourceList Blocks">
    <ListAllResourceListBlocks />
  </Story>
</Canvas>
